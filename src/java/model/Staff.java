package model;

public class Staff extends Member {
    
    // Hàm khởi tạo
    public Staff() {
        super();
    }
    
    // Sau này nếu Nhân viên (Staff) có thêm thuộc tính riêng
    // (ví dụ: String chucVu, double luong...) 
    // mà Member không có, bạn sẽ thêm vào đây.
}